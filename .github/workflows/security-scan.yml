name: Security & Code Quality Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Run weekly security scan on Mondays at 07:00 UTC
    - cron: '0 7 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  php-security-scan:
    name: PHP & WordPress Theme Security
    runs-on: ubuntu-latest

    steps:
      - name: Harden GitHub Actions Runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit

      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: composer, phpcs

      - name: Check PHP Syntax (Linting)
        run: |
          find . -maxdepth 3 -name "*.php" -not -path "*/node_modules/*" -not -path "*/dist/*" -not -path "*/.astro/*" -exec php -l {} +

      - name: Install WordPress Coding Standards
        run: |
          composer global config --no-plugins allow-plugins.dealerdirect/phpcodesniffer-composer-installer true
          composer global require --dev wp-coding-standards/wpcs dealerdirect/phpcodesniffer-composer-installer
          composer global exec -- phpcs -i

      - name: Scan WordPress PHP Code Security
        run: |
          composer global exec -- phpcs --standard=phpcs.xml

  npm-audit-scan:
    name: NPM Dependency Vulnerability Audit
    runs-on: ubuntu-latest

    steps:
      - name: Harden GitHub Actions Runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit

      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run Unit Tests
        run: npm test

      # `astro check` runs the content loaders, so it needs the live WordPress API.
      # Cloudflare's bot protection rejects GitHub runner IPs with a 403, so this step
      # requires the WP_BUILD_TOKEN secret plus a matching Cloudflare WAF skip rule.
      # TEMPORARY: non-blocking until that rule is in place. Remove continue-on-error
      # once the token is configured — a check that cannot fail is not a check.
      - name: Astro Type & A11y Verification
        run: npx astro check
        continue-on-error: true
        env:
          WP_BUILD_TOKEN: ${{ secrets.WP_BUILD_TOKEN }}

      - name: Run NPM Security Audit
        run: npm audit --audit-level=high

  workflow-security-scan:
    name: GitHub Actions Workflow Security
    runs-on: ubuntu-latest

    steps:
      - name: Harden GitHub Actions Runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit

      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Run Actionlint
        uses: reviewdog/action-actionlint@v1
        with:
          fail_level: error
